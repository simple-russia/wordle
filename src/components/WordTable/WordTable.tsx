import React from "react"
import styles from './wordtable.module.css';

interface iProps {}

const WordTable = ({}:iProps): JSX.Element => {
  return (
    <div className={styles.main}>
      word table here
    </div>
  )
};

export { WordTable }
